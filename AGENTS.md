# このプロジェクト内のルール

## 仕様ルール
- requirements/ 配下にある`spec.txt`からspecs/の各yamlファイルを生成する。
  - `spec.txt`は、アプリケーションの目的、満たすべき要件および基本動作を定義する初期要件とする。
  - `spec.txt`が空の場合は、要件が記載されるまで初回の仕様策定およびYAMLファイルの生成を行わない。
  - `spec.txt`の不明な要件やシステム構築において考慮不足な点があれば繰り返し質問して確認する。
  - `spec.txt`からYAMLファイルを生成する段階へ進むかどうかは人間が判断し、明示的に指示する。
  - YAMLファイルの生成後はspecs/配下のYAMLを正式仕様とし、人間から明示的な指示がない限り`spec.txt`を参照、更新または再検討しない。
  - 既にYAMLファイルが生成されている場合は、`spec.txt`が空でもYAMLを正式仕様として仕様変更および実装を継続する。
- 仕様作成時に一般的な理解で判断できるものはAIが自動で判断する。
  - 以下は合理的な範囲でAIが仕様案を作成してよい。
    - 一般的なCRUD仕様
    - 標準的なREST API設計
    - 一般的な入力チェック
    - エラーレスポンス形式
    - 一般的なログ出力
    - UI上の標準的な操作
    - 技術的に明白な補完事項
    判断した内容は事前承認を待たず仕様に直接反映し、判断内容を仕様に記録すること。
  - 以下についてはAIが独自に確定してはならない。
    - 課金に影響する仕様
    - セキュリティポリシー
    - データ保持期間
    - SLA
    - 権限モデルの変更
    - 大きなインフラ構成変更
    - 技術スタック変更
    - 外部サービス追加
    - 業務ルール
    - 要件から複数の合理的な選択肢が存在する事項
    - 技術スタックが未指定の場合の技術選定
    これらはAIが独自に確定せず、人間に確認すること。
- 質問や確認は1回での確認が多くならないように1度の確認は5項目までとする。
  5項目以上の質問や確認がある場合は、一度回答を得てから再度質問する。
- 影響の大きい質問を優先的に質問する。また、後続の仕様に影響を与える質問の場合は１回で１つのみの質問とする。
- specs/ 配下のYAMLを最終的な正式仕様とし、設計、実装、テストおよび環境構築はこれに従う。
- 仕様ファイルの拡張子は `.yaml` に統一する。
- 仕様は以下の５ファイルに分けて管理する。
  1.`01_overall_spec.yaml`,
  2.`02_data_spec.yaml`,
  3.`03_ui_spec.yaml`,
  4.`04_api_spec.yaml`,
  5.`05_infra_spec.yaml`
- 仕様の追加、削除、変更が生じた際にはspecs/ 配下のファイルを常に更新する。
- 要件が決まっていない部分は保留や未決事項として記載する。
- 保留・未決事項が残っていても、その影響を受けない範囲の仕様策定、設計、実装およびテストは進めてよい。影響を受ける範囲は、仕様IDによって明示する。
- 仕様変更や仕様追加時の各yamlファイルの修正は差分のみ修正しファイル全体を作り直さない。

### YAML共通記述ルール
- 各仕様項目には、ファイル間で一意に識別できる仕様IDを付与する。
- 各仕様項目には、状態を`確定`、`保留`、`未決`のいずれかで記録する。
- AIが補完または判断した仕様、および選択理由を残す必要がある仕様には、決定理由を記録する。
- 他の仕様項目に依存または影響する場合は、関連する仕様IDを記録する。
- 仕様IDは既存項目の更新時に変更せず、追跡可能性を維持する。
- YAMLの固定フォーマットは当面設けず、共通記述ルールを満たす範囲で案件ごとに最適な構造をAIが判断する。

### `01_overall_spec.yaml`に記載する内容
- システム全体に関する内容
- 必要な環境の種類（開発環境、テスト環境、ステージング環境、本番環境など）
- システムに必要なツール、ライブラリ
- システム構成の概要
- 定義された非機能要件の概要
- システム全体のセキュリティ要件
- 動作環境、対象ヴァージョンの概要
- インフラに関する詳細は`05_infra_spec.yaml`を正とし、関連する仕様IDを記載する。

### `02_data_spec.yaml`に記載する内容
- 永続化データのレイアウトと要件

### `03_ui_spec.yaml`に記載する内容
- システムで必要な画面と各画面の仕様
- 画面遷移
- UIに関する共通規定（フォント、色、配置）
- 画面内に配置するパーツの位置、色、機能
- UI系ライブラリ

### `04_api_spec.yaml`に記載する内容
- バックエンド側のAPI仕様
- 外部通信がある場合の外部接続に関するAPI仕様

### `05_infra_spec.yaml`に記載する内容
- 動作環境、ネットワーク、システム構成
- コンテナ化に関する情報
- 非機能要件への対策

### 仕様策定ループ
仕様を検討する際は以下の順序で処理する。
1. YAML生成前はrequirementsを確認する。YAML生成後は、人間から明示的に指示された場合に限りrequirementsを確認する。
2. specs全体を確認する。
3. 仕様間の依存関係を確認する。
4. 実装時に問題となる課題を抽出する。
5. 一般的な回答や解決策が存在する場合は仕様を補完する。
6. AI判断可能なものは事前承認を求めず、判断内容を仕様へ直接反映する。
7. 人間判断が必要なものは確認・質問する。
8. 仕様変更による影響範囲を確認する。
9. 関連するYAMLを更新する。
10. 再度仕様全体を確認する。

## 開発ルール
- 作業開始時は、`development_progress.yaml`が存在する場合は必ず確認し、完了済みフェーズ、現在のゲート状態、次の作業、未解決事項および検証結果を引き継ぐ。
- 複数フェーズにまたがる作業では、各フェーズの開始時、中断可能な区切り、完了時に`development_progress.yaml`を更新する。
- `development_progress.yaml`は作業再開用の参考情報とし、アプリケーションの正式仕様はspecs/配下のYAMLを正とする。両者が矛盾する場合はspecs/を優先する。
- プログラムに修正があったときは仕様も見直す。
- デザイン変更など、AI判断で構築した部分を人間が修正したときは、その修正内容を確定仕様として具体的にYAMLへ記録する。同じ箇所について将来AIが再判断できる曖昧な状態に戻してはならない。
- 開発上の試行錯誤は開発環境でのみ行う。
- execute/ フォルダに各環境用の起動シェルを生成する。
　起動シェルは環境構築タイミングで自動生成する。

### 実装完了の判定基準
- 関連する正式仕様が実装内容と一致するように更新されていること。
- 変更内容に対応するテストが追加または更新され、実行結果が正常であること。
- その作業で人間が構築または検証対象として指定した環境で、アプリケーションまたはサービスの起動を確認できること。指定されていない環境での確認は完了条件に含めない。
- 実装対象に影響する保留・未決事項が解消されていること。解消に人間の判断が必要な場合は確認すること。実装対象に影響しない保留・未決事項は完了を妨げない。
- テストまたは起動確認を実行できない場合は、未実施の理由、影響範囲および必要な対応を報告すること。

### データ変更ルール
- 永続化データの構造を変更する場合は、既存データへの影響、データ移行方法および後方互換性を確認し、`02_data_spec.yaml`へ記録する。
- データ消失、既存機能との非互換、または復旧困難な変更を伴う破壊的変更は、実施前に人間へ確認する。
- 必要なデータ移行手順を実装し、移行結果を検証する。

## 構築ルール
- 実際に環境構築が必要になった時点で、どの種類の環境を構築するかを人間に確認する。
- 必要な環境の種類は人間が決定する。
- 環境の種類が決定した後は、正式仕様であるYAMLに従い、その環境の通常の構築作業をAIが自動で実行する。
- 実装に必要な、承認済みの種類の環境は自動で構築する。
- 人間が対象として指定していない環境は、自動で構築、変更または起動確認しない。
- 既存のロックファイルに基づく依存関係の復元、および仕様・既存構成から必要性が明らかな一般的なライブラリの導入は、事前確認なしで実施してよい。
- ライブラリの導入に次のいずれかの懸念がある場合は、インストール前に確認する。
  - ライセンスまたは商用利用上の問題
  - メンテナンス停止、既知の重大な脆弱性、または提供元の信頼性の問題
  - 有料サービス、外部アカウント、システム権限、または広範なネットワークアクセスの必要性
  - 大規模なフレームワーク変更、既存ライブラリとの重大な競合、または大きな構成変更
  - 特殊・非一般的な依存関係による将来の保守負担
  - ロックファイルの大幅な変更、または多数の間接依存関係の追加
- フロントエンド、バックエンドに分ける場合は別フォルダで管理し
  それぞれで別のコンテナとして環境作成する。
- 各コンテナは起動時にサービスが自動的に起動する仕組みとする。
